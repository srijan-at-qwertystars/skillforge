# Review: bash-scripting-patterns

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 5/5
Overall: 5.0/5

Issues: none

Outstanding skill. Uses standard description format. Covers strict mode (set -euo pipefail), variable quoting rules, parameter expansion (defaults, require, pattern removal, replacement, case conversion), arrays (indexed + associative), conditionals ([[ ]] vs [ ], file/string/numeric), loops (for, while read, process substitution, subshell pitfall), functions (local, nameref), error handling (trap EXIT/ERR/signals, cleanup template, retry logic), input parsing (getopts + long options), file operations (mktemp, atomic writes, flock), ShellCheck warnings table, portability table (Bash vs POSIX), and anti-patterns (eval, parse ls, useless cat).
