# QA Review: mise-polyglot

**Skill path:** `~/skillforge/devops/mise-polyglot/`
**Reviewed:** 2025-07-17
**Reviewer:** Copilot QA

---

## a. Structure

| Criterion | Status | Notes |
|-----------|--------|-------|
| YAML frontmatter | ✅ Pass | `name` + `description` with positive and negative triggers |
| Under 500 lines | ✅ Pass | Exactly 500 lines |
| Imperative tone | ✅ Pass | Uses imperative throughout ("Install mise", "Add ONE of these") |
| Examples | ✅ Pass | Abundant copy-paste code blocks in every section |
| Linked resources | ✅ Pass | Tables linking `references/`, `scripts/`, `assets/` at bottom |

**Supporting files review:**
- `references/` (3 files): advanced-patterns.md, team-workflow.md, troubleshooting.md — all thorough and well-organized with TOCs
- `scripts/` (3 files): install-mise.sh, migrate-from-asdf.sh, mise-project-init.sh — functional scripts with error handling, help flags, color output
- `assets/` (3 files): mise.toml template, github-action.yml, dockerfile — well-commented, production-ready templates

---

## b. Content Accuracy (verified via web search)

| Item | Status | Notes |
|------|--------|-------|
| `mise use` syntax | ✅ Correct | `mise use node@20`, `-g` for global — verified |
| `mise install` syntax | ✅ Correct | No-arg installs from config, `mise install node@22` for specific |
| `mise ls` syntax | ✅ Correct | Lists installed tools; `mise ls-remote` for available |
| `mise run` syntax | ✅ Correct | `mise run task -- args`, shorthand `mise build` |
| `.mise.toml` format | ✅ Correct | TOML sections [tools], [env], [tasks], [settings], [hooks] |
| Backend names | ✅ Correct | core, aqua, github, cargo, npm, pipx, go, asdf — all valid |
| `github` backend (ex-ubi) | ✅ Correct | Correctly uses `github:owner/repo` (ubi is deprecated) |
| `jdx/mise-action@v2` | ✅ Correct | `install`, `cache`, `mise_toml` inputs all verified |
| Shell activation | ✅ Correct | bash/zsh/fish activation syntax verified |
| Version resolution order | ✅ Correct | .mise.toml > parent > global > legacy files |
| Legacy file support | ✅ Correct | .node-version, .nvmrc, .python-version, .tool-versions |
| Hooks | ⚠️ Minor issue | Line 324-328: duplicate `enter` key in same TOML block is invalid TOML. Should be shown as separate examples |
| `disable_backends` CLI | ⚠️ Minor issue | Line 301: `mise settings set disable_backends asdf` — actual syntax requires array: `mise settings set disable_backends '["asdf"]'` |
| `mise.lock` | ⚠️ Minor issue | Line 446: implies `mise.lock` is always generated by `mise use`, but it requires `[settings] lockfile = true` first |

**Overall accuracy:** Very high. Three minor issues found, none affecting core functionality.

---

## c. Trigger Check

### Positive triggers (pushy enough?)
✅ **Yes** — comprehensive list:
- Direct: `mise`, `rtx`, `.mise.toml`, `mise.toml`, `mise install`, `mise use`, `mise tasks`, `mise activate`, `mise hooks`
- Conceptual: polyglot tool version management, replacing asdf/nvm/pyenv/rbenv/direnv, project-level tool pinning, CI/CD with jdx/mise-action
- Adjacent: `.tool-versions` with mise, mise backends, mise env vars, mise task runner

### Negative triggers (false trigger risk?)
✅ **Well-bounded:**
- Docker/container orchestration, Kubernetes, Terraform/OpenTofu provisioning
- Ansible/Chef/Puppet config management
- Generic shell scripting unrelated to mise
- npm/pip/cargo as standalone package managers
- asdf without mise mention

### Potential improvements:
- Could add `mise exec`, `mise env`, `mise doctor`, `mise self-update` as positive triggers
- "polyglot tool version management" is slightly broad but acceptable in context
- Could add Nix/NixOS as a negative trigger to avoid overlap

---

## d. Scores

| Dimension | Score | Rationale |
|-----------|-------|-----------|
| **Accuracy** | 4 | Almost entirely correct. Three minor issues: duplicate TOML key in hooks example, simplified `disable_backends` CLI syntax, misleading `mise.lock` auto-generation claim |
| **Completeness** | 5 | Exceptionally thorough. Covers all mise features: installation, activation, tool management, config format, env vars, task runner (TOML + file-based), all 8 backends, legacy support, hooks, settings, comparison table, team patterns, CI/CD (GH Actions, GitLab, CircleCI, generic), pitfalls, quick reference. References, scripts, and assets are all high-quality |
| **Actionability** | 5 | Every section has copy-paste examples. Scripts are fully functional with flags, error handling, and color output. Templates are production-ready. Quick reference is excellent |
| **Trigger quality** | 4 | Good positive/negative balance. Covers mise + aliases comprehensively. Minor gaps in CLI subcommand triggers and potential Nix overlap |
| **Overall** | **4.5** | High-quality skill. Minor accuracy issues don't impact usability |

---

## e. GitHub Issues

**No issues filed.** Overall score (4.5) ≥ 4.0 and no dimension ≤ 2.

### Recommended improvements (non-blocking):

1. **Fix hooks example (SKILL.md L324-328):** Split duplicate `enter` key into separate example blocks or add a comment clarifying these are alternatives, not a single config
2. **Fix `disable_backends` syntax (SKILL.md L301):** Change `mise settings set disable_backends asdf` to `mise settings set disable_backends '["asdf"]'`
3. **Clarify `mise.lock` (SKILL.md L446):** Note that lockfile generation requires `[settings] lockfile = true`
4. **Expand triggers:** Add `mise exec`, `mise env`, `mise doctor` as positive triggers; add Nix/NixOS as negative trigger

---

## f. SKILL.md Annotation

`<!-- tested: pass -->`

---

**Review path:** `~/skillforge/reviews/devops-mise-polyglot.md`
**Result:** ✅ PASS
