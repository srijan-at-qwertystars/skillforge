# Review: deno-patterns
Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 4/5
Overall: 4.75/5
Issues: Non-standard description format. 501 lines (1 over limit, trivial).

Excellent Deno 2 guide. Covers fundamentals (secure by default, TypeScript native, Web APIs, npm compat), deno.json configuration (imports, tasks, compilerOptions, fmt, lint, lock, vendor, nodeModulesDir), module system (JSR, npm:, node:, URL imports, deno add), permissions (--allow-*/--deny-*, granular paths/hosts, runtime query, permission sets in deno.json), standard library (@std/ on JSR), HTTP server (Deno.serve, SSE streaming), Fresh framework (Preact islands architecture, file-based routing, route handlers, island components), Deno KV (CRUD, atomic check-and-set, queues, watch), npm compatibility (specifiers, package.json support, limitations), testing (Deno.test, subtests, snapshot, permissions, coverage), Deno Deploy (edge, KV, deployctl), CLI tools (compile, install, bench, doc, jupyter, publish), FFI (Deno.dlopen), Node.js migration (step-by-step, gotchas), and anti-patterns.
