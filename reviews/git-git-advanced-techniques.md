# Review: git-advanced-techniques

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 5/5
Overall: 5.0/5

Issues: none

Outstanding advanced Git guide. Standard description format. Covers bisect (manual/automated/skip), worktree (add/list/remove), rerere (enable/manage/gc), filter-repo (remove files/rename/mailmap/subdirectory extraction/replace text), sparse checkout (partial clone/cone mode/sparse-index), subtree (add/pull/push/split, vs submodules), advanced stash (partial/apply vs pop/branch from stash), blame forensics (-w -C -C -M/pickaxe -S/-G/follow renames), reflog mastery (recover lost commits/bad reset/expiration), Git internals (four object types/refs/packfiles/gc/fsck), cherry-pick (range/-x/conflicts), patch workflows (format-patch/am/apply), performance optimization (shallow+partial clone/commit-graph/multi-pack-index/fsmonitor), and anti-patterns.
