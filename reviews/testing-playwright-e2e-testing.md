# Review: playwright-e2e-testing

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 4/5
Overall: 4.75/5

Issues: Non-standard description format uses `positive:`/`negative:` sub-keys.

Outstanding Playwright skill covering setup and configuration (playwright.config.ts with projects, webServer, reporters), locator strategies (getByRole > getByLabel > getByPlaceholder > getByText > getByTestId > CSS/XPath), auto-waiting and assertions (auto-retrying matchers, expect.poll), Page Object Model (selectors in PO, assertions in tests), test fixtures (built-in, custom, worker-scoped), network interception (route/fulfill/abort/fetch+modify), authentication (storageState with setup projects), visual regression testing (toHaveScreenshot, masking, maxDiffPixelRatio), parallel execution and test isolation, debugging (--debug, --headed, --trace, --ui, codegen, page.pause), CI/CD (GitHub Actions with sharding, Docker), mobile/responsive testing, and comprehensive do/don't patterns.
