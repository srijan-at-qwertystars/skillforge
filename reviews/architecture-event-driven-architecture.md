# Review: event-driven-architecture

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 4/5
Overall: 4.75/5

Issues: Non-standard description format uses `positive:`/`negative:` sub-keys.

Outstanding skill. Covers events vs commands vs queries, event types (domain, integration, notification), event sourcing (append-only, state rebuilding, snapshots, projections), CQRS (separate read/write, eventual consistency mitigation), saga patterns (orchestration vs choreography with comparison table), outbox pattern (polling vs CDC vs listen/notify), event schema design (envelope, versioning strategies, schema registry), idempotent consumers (4 strategies), ordering guarantees and partitioning, error handling (DLQ, retry policies, circuit breakers), testing (aggregate, consumer contract, integration), and anti-patterns.
