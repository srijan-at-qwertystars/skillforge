# Review: svelte-patterns

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 4/5
Overall: 4.75/5

Issues: Non-standard description format (positive:/negative: sub-keys).

Excellent Svelte 5 + SvelteKit guide. Covers runes ($state with deep reactivity/$state.raw/$state.snapshot, $derived/$derived.by, $effect with cleanup, $props/$bindable/$inspect), reactivity (property-level tracking), component patterns (Snippets replacing slots, callback props, context, lifecycle), SvelteKit routing (file-based, dynamic, catch-all, groups), load functions (universal/server/streaming), form actions (fail/use:enhance/progressive enhancement), API routes, hooks (server handle/handleFetch/handleError), state management (runes in .svelte.ts), styling (scoped/:global), transitions/animations, SSR/SSG/SPA modes, deployment adapters, testing (Vitest/@testing-library/Playwright), and anti-patterns.
