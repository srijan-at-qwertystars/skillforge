# Review: react-state-management

Accuracy: 5/5
Completeness: 5/5
Actionability: 5/5
Trigger quality: 4/5
Overall: 4.75/5

Issues: Non-standard description format. 501 lines (1 over limit).

Outstanding skill with clear escalation path (useState → lifted → Context → URL → TanStack Query → Zustand/Jotai → XState). Covers built-in React state (useState, useReducer, useContext limits), Context optimization (split state/dispatch, split by domain, memoize, use-context-selector), Zustand (selectors, useShallow, slices, middleware), Jotai (atoms, derived, async, writable derived, atomFamily, splitAtom), TanStack Query v5 (useQuery, useSuspenseQuery, optimistic mutations, prefetching, select), Redux Toolkit (createSlice, RTK Query), XState v5 (setup, createMachine, fromPromise, useMachine), server vs client state, form state (React Hook Form + Zod), URL state (nuqs), performance patterns, and anti-patterns.
